# 🛡️ Q-Vault: Post-Quantum Hardware Security System
**Full Detailed Technical Documentation**

---

## 1. نظرة عامة على المشروع (Project Overview)
**Q-Vault** هو نظام أمني متكامل يهدف إلى حماية البيانات الحساسة باستخدام تقنيات تشفير ما بعد الكوانتم (Post-Quantum Cryptography). الفكرة الأساسية هي تحويل شريحة **ESP32** إلى "مفتاح فيزيائي (Hardware Key)" لا يمكن اختراقه، يُستخدم لفتح وقفل أقراص الويندوز المشفرة بواسطة **BitLocker** بشكل أوتوماتيكي ومخفي بالكامل (Zero-Trust & Zero-Knowledge).

النظام يتكون من **3 مشاريع منفصلة** تتكامل معاً في منظومة واحدة:
1. **Q-Vault-ESP:** السوفتوير (Firmware) الذي يعمل داخل الهاردوير (ESP32).
2. **Q-vault (Windows Service):** خدمة ويندوز مخفية تعمل كـ Middleware للتواصل بين الهاردوير ونظام التشغيل.
3. **PQC-Vault-GUI:** واجهة المستخدم الرسومية (GUI) لإدارة وتهيئة المفتاح والبارتشن.

---

## 2. التقنيات المستخدمة (Core Technologies)
*   **خوارزمية Kyber-768:** خوارزمية تبادل مفاتيح (KEM) مقاومة للحواسيب الكمية (Post-Quantum)، تستخدم لمنع هجمات الـ Man-In-The-Middle أثناء نقل الباسوورد عبر منفذ الـ USB (UART).
*   **Windows Management Instrumentation (WMI):** للتحكم العميق في إعدادات نظام BitLocker (تشفير، فتح، وقفل).
*   **Windows API (Win32):** لاكتشاف أجهزة الـ USB عند توصيلها أو إزالتها (DBT_DEVICEARRIVAL).
*   **BIP39 (NBitcoin):** لتوليد بذور (Seeds) من 12 كلمة واستخراج كلمات مرور حتمية (Deterministic) منها للحماية من فقدان الهاردوير.
*   **ESP-IDF / FreeRTOS:** نظام التشغيل المدمج لتشغيل الـ ESP32 وإدارة الذاكرة الآمنة (NVS) وتشغيل عمليات التشفير دون استهلاك الذاكرة (Memory leaks).

---

## 3. تفصيل مكونات النظام (System Components)

### أولاً: Q-Vault-ESP (هاردوير المفتاح)
يعمل هذا الكود بلغة **C** على متحكم ESP32، ويحتوي على وضعين للتشغيل:
1. **وضع التهيئة (Setup Mode):**
   *   يتم الدخول إليه إذا كانت الذاكرة (NVS) فارغة.
   *   يضيء الليد الأزرق بشكل ثابت.
   *   ينتظر استقبال أمر `SETUP|{Drive_GUID}|{Password}` من الـ GUI.
   *   يقوم بحفظ البيانات داخل الذاكرة ويعمل إيقاف تشغيل (Restart) للتحول إلى وضع الخزنة.
2. **وضع الخزنة (Vault Mode):**
   *   هو الوضع الافتراضي والأمني. المفتاح يظل صامتاً تماماً ولا يرسل أي بيانات.
   *   عند استقبال أمر `HELLO` من خدمة الويندوز، يرد بـ `READY`.
   *   يستقبل مفتاح التشفير العام (Kyber Public Key)، ويقوم بتشفير الـ (GUID و Password) وإرسالهم بأمان للكمبيوتر.
3. **وضع إعادة ضبط المصنع (Factory Reset):**
   *   لحماية النظام من إعادة البرمجة عن بُعد، يجب على المستخدم الضغط على زر الـ `BOOT` في الشريحة لمدة **5 ثوانٍ** لكي يتم مسح البيانات والعودة لوضع التهيئة.

### ثانياً: Q-vault (Windows Service)
هذه خدمة مكتوبة بـ **C/C++** تعمل بصلاحيات `SYSTEM` في الخلفية بمجرد تشغيل الويندوز.
*   **مراقبة المنافذ (Device Notification):** تراقب توصيل وفصل الـ USB. عندما يتم توصيل جهاز يحمل بصمة الـ ESP32 (`VID_1A86&PID_55D3`)، تبدأ العمل فوراً.
*   **المصافحة الآمنة (Secure Handshake):** تنفذ بروتوكول Kyber مع الـ ESP32 لضمان استلام كلمة المرور الخاصة بالبارتشن بشكل مشفر بالكامل.
*   **فتح البارتشن الأوتوماتيكي (Auto-Unlock):** بمجرد فك تشفير كلمة المرور في الرامات، تقوم الخدمة بفتح الـ BitLocker.
*   **بروتوكول الإغلاق (Lockdown Protocol):** عند سحب الـ ESP32 من الـ USB، تكتشف الخدمة ذلك وتقوم بعمل Lock فوري للبارتشن المفتوح لحماية البيانات في حالة ترك المستخدم للجهاز.

### ثالثاً: PQC-Vault-GUI (واجهة التحكم)
تطبيق مكتوب بـ **C# / WPF** بواجهة عصرية لتمكين المستخدم من إدارة الخزنة.
*   **التهيئة (Provisioning):** تقوم الواجهة بإنشاء كلمة مرور (أو توليدها من الـ Seed) ثم تشفير البارتشن باستخدام WMI (تحديداً `AES-256-XTS`).
*   **إدارة التعارض (Concurrency Management):** قبل تهيئة الـ ESP32، تقوم الواجهة بإيقاف الـ Windows Service مؤقتاً لضمان الحصول على وصول حصري لمنفذ الـ COM، ثم تعيد تشغيلها لاحقاً.
*   **الاسترجاع (Recovery):** تتيح للمستخدم إدخال الـ 12 كلمة (Seed)، وتقوم الواجهة رياضياً بحساب كلمة المرور الأصلية وتشفير أو فتح البارتشن وبرمجة ESP32 جديد دون الحاجة للمفتاح القديم.

---

## 4. دورة حياة العمليات (Workflows & Interaction)

### أ) دورة الـ Setup & Provisioning (عند إنشاء خزنة جديدة)
1. يختار المستخدم البارتشن من الـ GUI ويطلب إنشاء Seed جديد.
2. يضغط `PROVISION HARDWARE KEY`.
3. الواجهة (GUI) تقوم بإيقاف خدمة الويندوز `Q-vault` مؤقتاً.
4. تقوم الواجهة ببدء تشفير البارتشن بواسطة BitLocker (`EncryptDriveWithPassword`) في الخلفية.
5. تتصل الواجهة بالـ ESP32 وترسل له الـ GUID والباسوورد في أمر واحد `SETUP`.
6. الـ ESP32 يحفظهم، ويرسل `SETUP_SUCCESS`، ثم يعمل Restart.
7. الواجهة تعيد تشغيل الـ Service وتظهر للمستخدم رسالة النجاح.

### ب) دورة التشغيل اليومي (Daily Usage)
1. الكمبيوتر مغلق، أو المستخدم قام بتشغيل الجهاز للتو. البارتشن (SafeZone) مقفول.
2. يضع المستخدم הـ ESP32 في منفذ الـ USB.
3. تلتقط الـ Service (Q-vault) إشارة `DBT_DEVICEARRIVAL`.
4. ترسل الخدمة `HELLO` عبر הـ Serial Port.
5. يقوم הـ ESP32 بالرد بـ `READY`.
6. ترسل الخدمة 1184 بايت (Kyber Public Key).
7. يقوم הـ ESP32 بتشفير بيانات البارتشن باستخدام هذا المفتاح ويرسل 1372 بايت للكمبيوتر.
8. تقوم الخدمة بفك التشفير، واستخدام البيانات لعمل `UnlockWithPassphrase` للـ BitLocker.
9. يظهر البارتشن للمستخدم كأنه هارد طبيعي!

### ج) دورة سحب المفتاح (Key Removal)
1. يسحب المستخدم الـ ESP32 من الـ USB.
2. تلتقط الـ Service (Q-vault) إشارة `DBT_DEVICEREMOVECOMPLETE`.
3. تقوم الخدمة باستدعاء أمر `Lock` للـ BitLocker GUID الذي تم فتحه مسبقاً.
4. يختفي البارتشن فوراً.
5. يتم تنفيذ أمر `SecureZeroMemory` لمسح كل آثار الـ GUID والباسوورد من رامات الكمبيوتر بالكامل.

---

## 5. مبادئ الأمان (Security Posture)
1. **Zero-Knowledge Architecture:** لا يوجد أي مكان على الكمبيوتر (لا ريجستري ولا ملفات) يتم فيه حفظ كلمة السر للبارتشن.
2. **Post-Quantum Resistant:** حتى لو تم التنصت على منفذ الـ USB بواسطة جهاز خارجي (Hardware Sniffer)، البيانات المتبادلة مشفرة بخوارزمية Kyber التي لا يستطيع حتى الكمبيوتر الكمي كسرها.
3. **Ephemeral Memory:** كلمات المرور موجودة في الرامات لأجزاء من الثانية فقط أثناء الفك أو التشفير، ويتم إعدامها برمجياً بعد الاستخدام.
4. **Physical Ownership Verification:** لا يمكن عمل Factory Reset للـ ESP32 برمجياً من الـ GUI. يجب على المهاجم أو المستخدم الضغط الفيزيائي على زر الـ BOOT، مما يمنع البرمجيات الخبيثة من الاستيلاء على الخزنة.

---
**End of Document**
