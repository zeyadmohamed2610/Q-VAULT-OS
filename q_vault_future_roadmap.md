# 🚀 Q-Vault: Future Roadmap & Expansions
**وثيقة التوسعات المستقبلية والإضافات المخطط لها**

تم تصميم بنية **Q-Vault** الحالية لتكون قابلة للتطوير (Scalable). في هذه الوثيقة، نستعرض التحديثات الكبرى المخطط لإضافتها في الإصدارات القادمة، والتي ستنقل النظام من مجرد "مفتاح فيزيائي ذكي" إلى "منظومة أمنية محيطية متكاملة".

---

## 1. تطبيق الهاتف المحمول (Mobile Companion App)
سيتم تطوير تطبيق هاتف (Android / iOS) يعمل كجهاز تحكم ومصادقة إضافي (2FA & Remote Management) يتواصل لاسلكياً مع الـ ESP32 عبر الـ Bluetooth (BLE) ومع جهاز الكمبيوتر.

### مميزات التطبيق الأساسية:
*   **زر التدمير/الإغلاق عن بُعد (Kill Switch):**
    *   في حالة الطوارئ (مثال: تعرض الكمبيوتر للسرقة أثناء تشغيله والبارتشن مفتوح)، يمكن للمستخدم بضغطة زر من التطبيق إرسال إشارة للـ ESP32.
    *   الـ ESP32 بدوره يرسل إشارة طوارئ للـ Windows Service لتقوم فوراً بعمل `Lock` للبارتشن، ومسح رامات الكمبيوتر (`SecureZeroMemory`)، وإلغاء مصادقة المفتاح حتى يتم تفعيله يدوياً مرة أخرى.
*   **نظام المناطق الآمنة (Safe Zone & Unsafe Zone):**
    *   **Safe Zone (المنطقة الآمنة):** عبر التطبيق، يمكن للمستخدم تحديد شبكات Wi-Fi معينة (عبر الـ MAC Address/BSSID) أو نطاق بلوتوث معين كمكان آمن (مثال: مكتب العمل أو المنزل). النظام سيسمح للـ ESP32 بفك تشفير البارتشن **فقط** إذا كان داخل هذه المنطقة.
    *   **Unsafe Zone (المنطقة غير الآمنة):** إذا تم توصيل الـ ESP32 بجهاز الكمبيوتر في كافيه أو شبكة عامة (أو تم اختطاف الجهاز بعيداً عن هاتف المستخدم)، فإن النظام سيكتشف أنه في Unsafe Zone ولن يرسل كلمة المرور نهائياً حتى لو كان المفتاح الفيزيائي متصلاً، مما يضيف طبقة حماية بيئية (Environmental Fingerprinting).

---

## 2. دمج VeraCrypt كنظام تشفير موازي لـ BitLocker
حالياً، يعتمد النظام على `Windows BitLocker` نظراً لتكامله السلس مع نظام التشغيل عبر الـ WMI. التحديث القادم سيتضمن دعم محرك **VeraCrypt** مفتوح المصدر كنظام تشفير إضافي أو بديل للـ BitLocker.

### لماذا VeraCrypt؟ (مميزات VeraCrypt بجانب BitLocker):
1. **الإنكار المعقول (Plausible Deniability - Hidden Volumes):**
   *   *أهم ميزة:* يتيح VeraCrypt إنشاء "خزنة مخفية" داخل الخزنة العادية. إذا تم إجبار المستخدم تحت التهديد على إعطاء الـ ESP32 أو الـ Seed الخاص به، سيفتح النظام الخزنة العادية (التي تحتوي على ملفات وهمية أو غير حساسة)، بينما تظل الخزنة المخفية الحقيقية غير مرئية تماماً ولا يمكن إثبات وجودها رياضياً.
2. **الاستقلالية والشفافية (Open Source & No Backdoors):**
   *   BitLocker هو نظام مغلق المصدر (Closed Source) تملكه Microsoft. بينما VeraCrypt مفتوح المصدر وتم تدقيقه أمنياً عدة مرات، مما يعطي ثقة مطلقة للمستخدمين الذين يخشون وجود ثغرات حكومية (Backdoors).
3. **خوارزميات التشفير المتداخلة (Cascaded Encryption):**
   *   BitLocker يدعم خوارزمية واحدة في نفس الوقت (مثل AES-256-XTS). VeraCrypt يتيح تشفير البيانات باستخدام 3 خوارزميات فوق بعضها (AES + Twofish + Serpent)، مما يجعل محاولة كسرها شبه مستحيلة حتى في المستقبل البعيد.
4. **دعم المنصات المتعددة (Cross-Platform):**
   *   دعم VeraCrypt سيمهد الطريق لفتح خزنة Q-Vault على أجهزة Linux أو macOS في المستقبل، عكس BitLocker المقتصر على بيئة Windows.

---

## 3. التعديلات المعمارية المطلوبة لدعم الإضافات (Architectural Changes)
لتنفيذ هذا الـ Roadmap، سيتم تحديث الـ 3 مشاريع كالتالي:

1. **في Q-Vault-ESP:**
   *   تفعيل مكدس البلوتوث (BLE Stack) للتواصل المشفر مع تطبيق الموبايل.
   *   إضافة جداول لحفظ إعدادات الـ Safe Zones (BSSIDs).
2. **في Q-Vault Windows Service:**
   *   إضافة وحدة (Module) جديدة مخصصة للتعامل مع سطر أوامر VeraCrypt (`VeraCrypt Format.exe /v`) بجانب وحدة الـ BitLocker الحالية.
3. **في PQC-Vault-GUI:**
   *   إضافة واجهة لاختيار محرك التشفير المفضل للمستخدم عند الـ Provisioning (مربع اختيار بين `BitLocker` أو `VeraCrypt`).
   *   توفير واجهة لضبط الخزنة المخفية (Hidden Volume).

---
**End of Roadmap Document**
